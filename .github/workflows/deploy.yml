name: Deployment
on:
  push:
    branches:
      - main
      - dev
env:
  MONGODB_DB_NAME: gha-demo
jobs:
  test:
    env: 
      MONGODB_CLUSTER_ADDRESS: cluster0.eqouwtb.mongodb.net
      MONGODB_USERNAME: vigneshk1121
      MONGODB_PASSWORD: 3AuftJS@jWuCUYH
      PORT: 8080
      #MONGODB_PASSWORD: Tx45pN7oDF9AbLsM
    environment: testing
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v3
      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: npm-deps-${{ hashFiles('**/package-lock.json') }}
      - name: Install dependencies
        run: npm ci
      - name: Run server
        run: npm start & npx wait-on http://127.0.0.1:$PORT # interpolate a env values in run command
      - name: Run tests
        run: npm test
      - name: Output information
        run: echo "MONGODB_USERNAME ${{env.MONGODB_USERNAME}}"
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: |        
          echo "..."
